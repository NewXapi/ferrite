## What

把 admin 控制面与 gateway 数据面之间仍在断裂的三处接线补上：`POST /api/gateway/reload` 从 501 占位改成真实热更实现并补 admin 守卫；`usage.rs` 的消费日志 `log_type` 从手写 1 改回 2 并常量化读写两侧；启动顺序改为 migrations 先于 `load_proxy_snapshot`。

> 本 PR 原含 5 项修复；其中 `api_channels` 分组列对齐与 dev seed 部分已被 #119 / #118 以更彻底的方案（wire 直接改 `groups: Vec<String>` 数组端到端）覆盖，已从本分支移除，以 #119 为准。

## Why

三个缺陷让网关在真实流量下不工作或不可运维，而且都不是「缺功能」，是现有代码互相打架。

`usage.rs` 手写 `log_type: 1` 但注释写着 consume；枚举是 1=充值 2=消费，而 `/api/log/top` 与 `/api/log/trend` 都过滤 `log_type = 2`，所以网关记录的每一条真实消费都被排行榜和趋势图丢掉——有流量也白记。

`load_proxy_snapshot` 查 `proxy_nodes` 表早于 `run_migrations` 建表，空库首次启动直接失败。

reload 端点是 501 占位且无守卫，但 main 已有断言它返回 200 带计数的 e2e 测试——因为 CI 没有 postgres service，这个测试一直静默跳过。本 PR 实现热更（Shared\* store + `Dispatcher::set_snapshot`）并补 401/403 守卫。

## Issue

无关联 issue（PR-only 流程）；承接 gateway 审计会话结论。

## Construction plan

- [x] T3 `usage.rs` log_type 常量化（写侧走 `UsageEvent::consume` 构造器）+ 回归测试
- [x] T4 启动顺序：migrations 先于 `load_proxy_snapshot`
- [x] T5 reload 真实现 + admin 守卫，替换 Dispatcher 与三份 gate 快照
- [x] CRG + ocr 审查（历史轮见 comment；rebase 后增量待 CI）
- [x] 本地真 PG smoke（见 comment）

## Delivery record

worktree `.wt/gateway-dataplane` · rebase 后 base `0043471` · 4 commits（85e89bd → 3afcc13）

- 85e89bd T4 启动顺序 · 62e0bd6 T3 log_type 常量化 · d26f79d T5 reload 热更 · 3afcc13 ocr 修复（clamp + 文档）
- 验收：e2e `admin_gateway_flow` 6/6 + `usage_log_type` 3/3（真 PG）；`cargo check` 4 crate 干净
- CRG：0 受影响 flow；ocr：3 采纳修复已含（多组截断 warn 部分因 #119 数组化而作废，仅保留 usage clamp）
- 计划外改动（d26f79d，均为恢复既有契约）：`assemble` 增加 `wire_proxy_pool` 参数修复 #108 起 mock egress 被代理池绕过；新增 `gateway_path_guard` 让未匹配路径返回 404 而非 AuthGate 401

## How to test

```sh
cpulimit -l 70 -i -- cargo check -p api -p admin-router -p observe --all-targets
FERRITE_JWT_SECRET=<32+字节> FERRITE_E2E_DATABASE_URL=postgres://ferrite:ferrite@127.0.0.1:5433/ferrite_e2e \
  cpulimit -l 70 -i -- cargo test -p tests-e2e --test admin_gateway_flow -- --test-threads=1
```

真实路径：插新 Token → `POST /api/gateway/reload`（admin JWT）应 200 带计数，且新 Token 立即可过 `/v1/chat/completions` 而无需重启；无头/非 admin 调 reload 得 401/403。

## Checklist

- [x] 消费日志进入 `/api/log/top` 与 `/api/log/trend`（回归测试钉住）
- [x] 空库首次启动成功（migrations 先行）
- [x] reload 后新 Token 生效且用量中间件看到同一份快照
- [x] reload 有 401/403 负路径测试
- [x] CRG / ocr findings 全部处置

## Suspect area

- `apps/api/src/usage.rs` 用量中间件在 pipeline 外，`channel_key`/`channel_name` 恒空——后续 PR 以 request extensions 回传路由归因解决
- quota 桶 `parse::<i64>().unwrap_or(0)` 全塌成 `"0"`，已知 `TODO(#211)`，另有 PR 处理
- reload 的四次 store 非原子（单机管理面可接受）；`Snapshots.dispatch` boot 后陈旧（reload 走 `set_snapshot`），均有 doc 注释

## Out of scope

`crates/api/admin-proxy/`、`crates/gateway/proxy/`（另开 PR）、`crates/web/**`、`crates/contract/**`（#119 已定数组方案）。

## Cross-domain note

涉及 `apps/api`、`crates/api/admin-observe`（logs.rs 常量导出）、`crates/api/admin-router`（auth_svc 注入反转）、`crates/gateway`（仅依赖既有 pub API，无改动）、`tests/`。开工前已查在跑 PR：#120-#123（web ui 域）、#119/#118（catalog/seed，已 merge）、#117（seed 瘦身，已 merge），无冲突会话。
