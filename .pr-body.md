## What

删除 4 个随 squash merge 泄漏进 main 的会话工作文件：`.pr-body.md`、`.pr-body-current.md`、`.pr-comment-review.md`、`.pr-comment-smoke.md`（来自 #116 与 #132，内容由某个子任务的 `git add -A` 扫入分支）。它们是 PR body 与审查/验证留言的工作底稿，正式版本已在 GitHub 的 PR 页面，仓库里留副本纯属泄露。

## Why

工作文件不该进版本库：内容与 PR 页面重复、随会话过期、还会污染 `git status`。根因是子代理提交时用 `git add -A` 未做边界自查、主控审计时只核对了声明的源文件漏了未跟踪文件——本 PR 修表，审计流程改为「diff 边界核对必须包含 untracked 清单」。

## Issue

无关联 issue（PR-only 流程）；#116 / #132 的收尾清理。

## Construction plan

- [x] `git rm` 4 个文件（纯删除，无代码改动）
- [x] 复核 main 树无其他同源泄露（`git ls-tree newxapi/main` 仅此 4 项）

## Delivery record

worktree `.wt/pr-files-cleanup` · base `123e5dc` · 1 commit（7766522）· 净删 194 行

## How to test

```sh
git ls-tree newxapi/main --name-only | grep -c pr-   # merge 后应为 0
```

## Checklist

- [x] 纯删除，无代码/文档语义变化
- [x] 泄露根因已在审计流程中闭环（untracked 纳入边界核对）

## Suspect area

无——删除对象均已确认在 PR 页面有正式副本。

## Out of scope

无其他改动。

## Cross-domain note

无跨域（仅根目录 4 个工作文件）。开工前查过在跑 PR：无重叠。
