## Issue

PR-only 开发，无新建 issue。admin-api 控制面 tracking（#11）已随 #17 合并关闭，本 PR 为其后续批次。

## What
admin-api 第四批：渠道探活（单渠道 + 全量测试，monitor_history 落库与可用率查询）、admin 用户单查/搜索、token 明文补取（admin/owner 场景）。

## Why
README 路线图 P0 缺口：渠道探活是渠道管理闭环的最后一环；admin-web 渠道页需要测试按钮，用户页需要单查/搜索，token 页需要重新查看 key 的能力（对齐 new-api GetTokenKey）。

## Construction plan
- [ ] observe/monitor: monitor_history 平表 + record_probe + availability 查询（替换 TODO 骨架）
- [ ] catalog/channels: test_channel（reqwest 真实调用 test_model/chat completions，短超时短输出）+ test_all
- [ ] 路由: POST /api/channel/{key}/test、POST /api/channel/test、GET /api/monitor/{key}?days=7
- [ ] auth: GET /api/user/search（admin）、GET /api/user/{key}（admin）
- [ ] catalog/tokens: POST /api/token/{key}/key（owner/admin 重取明文 — 需可逆存储决策）

## Suspected areas
- catalog 依赖 reqwest（探活 HTTP 客户端；workspace 已有）
- observe/monitor.rs 骨架被替换
- 不触碰: apps/api、crates/gateway、store trait

## Done when
- [ ] cargo test -p observe -p catalog --tests -- --include-ignored 全绿
- [ ] cargo clippy 新代码零警告
- [ ] gate pre-commit ALL PASS

## Out of scope
ops::jobs 定时调度、billing 兑换码、2FA/session 列表

## How to test
DATABASE_URL=... cpulimit -l 70 -i -- cargo test -p observe -p catalog --tests -- --include-ignored

## Checklist
- [ ] 测试通过且覆盖新增行为
- [ ] clippy 无新增告警
- [ ] 审查 findings 已修或记录裁决

## Delivery record
- 分支 feat/admin-api-clean（自 newxapi/main 切出），draft 起步，批次完成逐项勾回
- Worktree: .wt/admin-api
