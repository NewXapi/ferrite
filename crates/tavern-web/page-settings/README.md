# `tavern-page-settings`

## src/lib.rs

- `SettingsPage` — 读取和保存连接、模型、temperature、top_p、max_tokens。
- `ConnectionForm` — base URL、模型名和 API key 输入。
- `SamplerForm` — 采样参数输入。
- `ModelsList` — 显示 `/v1/models` 返回的模型。

