[package]
name = "store"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# 存储端口 + 两个实现。业务代码只面对 trait, 不感知 backend。
# DDL (migrations/) 与 Fjall key encoding 都锁在本 crate 内。

[dependencies]
contract = { path = "../../contract" }
serde    = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# TODO(#400): 实现期才引入 — sqlx (PG) 与 fjall 先注释掉, 骨架阶段零外部依赖
# sqlx  = { workspace = true }
# fjall = { version = "2" }
