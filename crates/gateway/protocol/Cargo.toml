[package]
name = "protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# 协议层 — 对应 new-api modules/relaykit 的角色, 但自主命名。
# 职责: 规范化协议模型 + 格式转换引擎 + SSE 扫描/封帧。host 无关:
# 只有 serde/bytes, 不引 tokio/reqwest/axum, 所以也能被 console 复用。
# 参考: new-api apps/api/modules/relaykit (request_registry/response_registry/convmeta)。

[dependencies]
contract = { path = "../../contract" }
serde    = { workspace = true }
serde_json = { workspace = true }
bytes    = { workspace = true }
thiserror = { workspace = true }
