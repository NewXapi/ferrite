[package]
name = "gateway-proxy"
version = "0.1.0"
edition = "2024"
publish = false

# 出口代理节点：解析 + 按 channel_key 选节点 + 租约 Client。HTTP/SOCKS 握手交给 reqwest。

[dependencies]
reqwest   = { workspace = true }
url       = "2"
percent-encoding = "2"
thiserror = { workspace = true }
arc-swap  = { workspace = true }
rand      = { workspace = true }
