[package]
name = "gateway-proxy"
version = "0.1.0"
edition = "2024"
publish = false

# 出口代理节点：配置解析 + 按 channel 选节点 + SSRF 校验。
# 拨号不在此 crate —— 见 lib.rs 模块文档。

[dependencies]
reqwest   = { workspace = true }
url       = "2"
percent-encoding = "2"
thiserror = { workspace = true }
arc-swap  = { workspace = true }
rand      = { workspace = true }
