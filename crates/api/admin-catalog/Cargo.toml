[package]
name = "catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# 配置权威 — 渠道/模型/分组/token/路由单元的全部业务写操作。
# 依赖 contract (DTO) 与 auth (用户角色校验)。

[dependencies]
contract = { path = "../../contract" }
auth     = { path = "../auth" }
observe  = { path = "../admin-observe" }
reqwest  = { workspace = true }
axum     = { workspace = true }
sqlx     = { workspace = true }
serde    = { workspace = true }
serde_json = { workspace = true }
chrono   = { workspace = true }
uuid     = { workspace = true }
rand     = { workspace = true }
sha2     = { workspace = true }
hex      = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
