[package]
name = "db-bootstrap"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Single bootstrap entry for db/migrations"

[dependencies]
sqlx = { workspace = true, features = ["migrate"] }
