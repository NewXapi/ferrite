[package]
name = "ops"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# 运维域 — 持久化任务 runner / 设置 / 通知 / 探活 (center)。
# 参考: new-api internal/ops (store_system_task/schedule_tasks/manage_options/
# notify_user/probe_channel), sub2api (FOR UPDATE SKIP LOCKED 任务认领 +
# advisory lock 选主 — 正是我们无 Redis 方案的替代)。

[dependencies]
axum     = { workspace = true }
sqlx     = { workspace = true }
serde    = { workspace = true }
serde_json = { workspace = true }
uuid     = { workspace = true }
thiserror = { workspace = true }
auth     = { path = "../auth" }
