## Smoke 验证通过

**手段**：本机真实 PG（docker `uf-local-postgres:5433`），不用任何 mock 替身跑管理面路径。

### 1. 全套 e2e（真 PG，`FERRITE_E2E_DATABASE_URL=...ferrite_e2e`）

| 套件 | 结果 |
|---|---|
| `admin_gateway_flow` | **6/6 绿**，含核心验收 `e2e_reload_picks_up_new_token_without_restart`：401 → 插库 → `POST /api/gateway/reload` 200 带计数 → 同一 app 实例上 `/v1/chat/completions` 200 + usage 落库。即热重载闭环真跑通 |
| `gateway_e2e` | **9/9 绿**（含 proxy pool 接线/不接线的两条 seam 测试） |
| `auth_flow` | **3/3 绿** |
| `usage_log_type`（本 PR 新增） | **3/3 绿**：构造 → `record` 落库 → `top_usage`/`trend` 均可查到该条消费 |
| `catalog channels_groups`（`--ignored`） | **11/11 绿**（roundtrip/空分组/COALESCE/多组截断/引用计数） |
| `api reload_counts`（离线单测） | **2/2 绿** |

### 2. 数据库真实路径（psql 一次性库）

- 迁移 0001–0005 全部 `ON_ERROR_STOP=1` 灌入成功
- **整份 seed.sql 灌入成功**，`SELECT name, groups, status FROM api_channels` → 4 行全部 `{default}`——种子与迁移后 schema 一致（修复前 `group_name` 列不存在直接报错）

### 3. 修复前后的行为对照（本次 PR 的意义）

- 修复前：迁移库上渠道 CRUD `column "group_name" does not exist` 500；`groups` 恒 `'{}'` → route_units 0 → `/v1/*` 404 `no_route`；消费日志全标充值被 top/trend 过滤；reload 501 且无守卫
- 修复后：上表全绿；reload 有 401/403 守卫负路径测试覆盖；未匹配路径 404 而非 AuthGate 401

**CI 注意**：CI 无 postgres service，上述 e2e 在 CI 里会静默 skip（这也是 main 上 reload 测试长期掩盖 501 的原因）。本 PR 的行为验证以本条 smoke 为准。
