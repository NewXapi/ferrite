## What

把 admin 控制面与 gateway 数据面之间断裂的接线补上，共 5 处：`api_channels` 分组列从已被迁移删除的 `group_name` 对齐到 `groups TEXT[]`；`usage.rs` 的消费日志 `log_type` 从 1 改回 2；启动顺序改为 migrations 先于 `load_proxy_snapshot`；`POST /api/gateway/reload` 从 501 占位改成真实热更实现并补 admin 守卫；dev seed 与 e2e 测试的插入语句同步到新列。

## Why

三个缺陷让全新部署的网关完全不通，而且都不是「缺功能」，是现有代码互相打架。

迁移 `0001` 已 `DROP COLUMN group_name` 并改用 `groups TEXT[]`，`apps/api/src/snapshot.rs:78` 跟上了，但 `admin-catalog/src/channels.rs` 仍在读写旧列。实测在跑过迁移的库上渠道 CRUD 全线 500，连带 `groups` 永无写入方恒为 `'{}'`，`snapshot.rs:165-171` 的空 groups 短路使 route_units 为 0，**每个 `/v1/*` 请求返回 404 no_route**。

`usage.rs:209` 写 `log_type: 1` 但注释写着 consume；枚举是 1=充值 2=消费，而 `/api/log/top` 与 `/api/log/trend` 都过滤 `log_type = 2`，所以网关记录的每一条真实消费都被排行榜和趋势图丢掉。

`load_proxy_snapshot` 查 `proxy_nodes` 表早于 `run_migrations` 建表，空库首次启动直接失败。

reload 端点是 501 占位，但 main 的 284d54a 已经加了断言它返回 200 带计数的 e2e 测试——因为 CI 没有 postgres service，这个测试一直静默跳过。

## Issue

无关联 issue（PR-only 流程）；承接 gateway 审计会话结论，与 #110 的 `groups TEXT[]` 演进对齐。

## Construction plan

- [x] T1 `api_channels` 分组列对齐 `groups TEXT[]`（阻塞级）
- [x] T2 dev seed 与 e2e 测试插入语句对齐 groups 列
- [x] T3 `usage.rs` log_type 1 改 2 + 回归测试
- [x] T4 启动顺序：migrations 先于 `load_proxy_snapshot`
- [x] T5 reload 真实现 + admin 守卫，替换 Dispatcher 与四份 gate 快照
- [x] CRG + ocr 审查
- [x] 本地真 PG smoke：迁移 + 建渠道 + `/v1/*` 打通

## Delivery record

worktree `.wt/gateway-dataplane` · base `afc154b` · 7 commits（87d5066 → 085720c）

- T4 启动顺序 87d5066 · T1 groups TEXT[] 586a9ce · T3 log_type 常量化 20b0d53 · T2 seed 对齐 18eb364 · T5 reload 热更 78d8cae · fmt 275005b · ocr 修复 085720c
- 验收：catalog `channels_groups` 11/11、`api reload_counts` 2/2、e2e `admin_gateway_flow` 6/6 + `gateway_e2e` 9/9 + `auth_flow` 3/3、`usage_log_type` 3/3（均真 PG）；`cargo clippy -D warnings`（5 crate）与 `cargo fmt --all --check` 干净
- CRG：16 文件、0 受影响 flow、risk 0.40；ocr：5 批 14 findings，3 采纳修复（085720c），余驳回附理由（见 review comment）
- smoke：整份 seed.sql 在迁移后一次性库灌入成功，4 渠道 groups={default}（见 smoke comment）
- 计划外改动（78d8cae，均为恢复既有契约）：`assemble` 增加 `wire_proxy_pool` 参数修复 #108 起 mock egress 被代理池绕过；新增 `gateway_path_guard` 让未匹配路径返回 404 而非 AuthGate 401

## How to test

```sh
cpulimit -l 70 -i -- cargo check -p catalog -p api --all-targets
FERRITE_E2E_DATABASE_URL=postgres://ferrite:ferrite@127.0.0.1:5433/ferrite_e2e \
  cpulimit -l 70 -i -- cargo test -p tests-e2e --test admin_gateway_flow
```

真实路径：跑完 0001-0005 迁移的干净库上建渠道并绑定分组，建 Token，调 `/v1/chat/completions` 应返回 200 而非 404 no_route；插新 Token 后 `POST /api/gateway/reload` 应返回 200 带计数，且新 Token 立即可用而无需重启。

## Checklist

- [x] 渠道 CRUD 在跑过迁移的库上不再 500
- [x] `/v1/*` 在干净库上可打通（route_units 非空）
- [x] 消费日志进入 `/api/log/top` 与 `/api/log/trend`
- [x] 空库首次启动成功
- [x] reload 后新 Token 生效且用量中间件看到同一份快照
- [x] CRG / ocr findings 全部处置

## Suspect area

- `apps/api/src/usage.rs:214` 用量中间件在 pipeline 外，拿不到 `ctx.route`，`channel_key` / `channel_name` 恒空——本 PR 不修，需要把记录移进 pipeline，范围过大另开
- quota 桶 `parse::<i64>().unwrap_or(0)` 全塌成 `"0"`，已知 `TODO(#211)`，本 PR 不动
- 代理 key 口径 UUID 与 name 不一致导致代理静默降级直连，属 PR #111 域，不动
- `ChannelView` / `ChannelRow` 是导出形状，改字段需查 web 引用；284d54a 刚把 channels 页接上真实 API，需确认不打断
- reload 要替换 `Arc<Snapshots>`，而 usage 中间件持有其副本，两边必须看到同一份

## Out of scope

`crates/api/admin-proxy/` 与 `crates/gateway/proxy/`（PR #111 独占）、`admin-catalog/src/routes.rs` 与迁移 0006（PR #114 独占）、`crates/web/**`（维护者自行做 UI）。

## Cross-domain note

涉及 `crates/api/admin-catalog`、`apps/api`、`db/dev`、`tests`。开工前已查在跑 PR：#111 独占 proxy 域、#114 独占 route_units、#113 纯文档，均无重叠。

