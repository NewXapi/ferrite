## Tool review（CRG 结构层 + ocr 规范层）结论

**CRG** `detect-changes --base afc154b`：16 文件、5 函数变更、**0 受影响 flow**、risk 0.40。2 个 test gap 均为测试辅助函数（`pg_pool`/`sample_job`）误报——它们被同文件测试直接调用。

**ocr** 按提交分 5 批（每批单主题），逐条裁决如下。

### 采纳并修复（commit 085720c）

| 位置 | 级别 | 处置 |
|---|---|---|
| `channels.rs` `groups_to_group_name` | maintainability·medium | 多分组行读出截断为首元素时落 `warn!`，让临时性的单值 wire 语义在生产日志可见 |
| `usage.rs` `build_consume_event` | bug·high | token 计数 i64→i32 由裸 `as` 改 `clamp`，异常计数饱和而非回绕成负数污染 `sum` 聚合 |
| `usage.rs` 文档措辞 | documentation·low | 写明 `log_type` 由 `consume()` 构造函数内部设置，非本函数手写 |

### 驳回（附理由）

- **「新测试标 `#[ignore]` 永不进 CI」(test·high)** — 该文件 main 上既有 6 个测试全部 `#[ignore]`（PG 集成测试、模块头注明运行命令），4 个 catalog 测试文件均此约定；本 PR 新测试沿用同一约定。
- **「snapshot.rs 非原子四次 store」/「`Snapshots.dispatch` boot 后陈旧」(other·high ×2)** — 两者均为**设计文档明示的已接受权衡**：单机管理面 reload 频率下，混合可见窗口可接受；`.dispatch` 仅 boot 读，reload 走 `Dispatcher::set_snapshot`。doc 注释已在 78d8cae 落明。
- **中文注释/doc (style·high ×2)** — 中英混排为本仓约定（#114 审查同款 finding 已有先例驳回）。
- **`RecordJob` 全字段 pub 仅服务测试 (maintainability·medium)** — tests-e2e 是仓内 crate 非外部 API，doc 已注明「不作为稳定 API」；收窄 facade 只增样板。
- **Arc clone 模式 / shared() 单行 / 测试 fixture 命名 / 中英混排断言消息 (low 若干)** — 惯用写法或既有模式，`Arc` clone 零成本。

### 覆盖说明

78d8cae 的 5 个文件中 4 个完成审查（1 项反复超时未完成，为 `admin_gateway_flow.rs`——主控已人工通读其全部断言并真跑 6/6 通过）。修复后 `cargo check -p catalog -p api --all-targets` 绿、`usage_log_type` 3/3 绿、`cargo fmt --all --check` 干净。
